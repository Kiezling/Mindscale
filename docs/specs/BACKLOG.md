# MindScale spec backlog

This file contains only unstarted, non-urgent work. Active work belongs in `PROJECT_STATE.md`; approved implementation details belong in a `SPEC-*.md` file.

<!-- Add entries in this form:
- [ ] Short outcome — why it matters; dependencies or decision needed.
-->

No unstarted scoped work. The paced-breathing object moved to active work on 2026-08-05 and
is governed by `docs/specs/SPEC-paced-breathing.md`; `PROJECT_STATE.md` owns it while it is
in progress.

The later UI overhaul is deliberately not listed here. It has no agreed outcome, dependency,
or decision yet, and an entry without those would be a placeholder rather than a backlog
item. Scope it before adding it.
